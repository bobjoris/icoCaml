digraph TEST/bignum.p
{  bigmult -> bigadd;
  bigmult -> smallmult;
  bigmult -> copy;
  bigmult -> grandir;
  bigfact -> smallmult;
  bigfact -> grandir;
  affiche -> writeln;
  affiche -> write;
  program -> bigfact;
  program -> readln;
  program -> affiche;
  program -> pow;
  program -> grandir;
  smallmult -> modulo;
  pow -> affiche;
  pow -> writeln;
  pow -> bigmult;
  pow -> pow;
  pow -> grandir;
  pow -> copy;
  grandir -> modulo;
  bigadd -> modulo;
}
